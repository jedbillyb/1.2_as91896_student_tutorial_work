## This program calculates GST
# print("This program calculates GST")
# cost_price = int(input("What is the cost of the item excluding GST "))
# GST_RATE = 1.15
# gst_inclusive = cost_price*GST_RATE
# print("the GST exclusive price is", cost_price, "when you add GST it costs", gst_inclusive)


## This program calculates GST
# print("This program calculates GST")
# cost_price = int(input("What is the cost of the item excluding GST "))
# GST_RATE = 1.15
# gst_inclusive = round(cost_price*GST_RATE)
# print("The GST exclusive price is", cost_price, "when you add GST it costs", gst_inclusive)

# This program calculates swimming pool deminsions
print("This program calculates swimming pool deminsions")
length = int(input("What is the length of the pool in metres? "))
width = round(length*0.4)
print(f"The width of the pool is {width} metres, and the length is {length} metres")
